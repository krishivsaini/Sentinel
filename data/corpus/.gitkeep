# Corpus lands here (§6): IETF RFC web-protocol stack, plain-text from rfc-editor.org.
# Populated in Phase 1, Day 2. RFC text is committed verbatim — the IETF Trust permits
# whole-RFC reproduction (see docs/requirement.md §5). Do NOT modify RFC text (no derivatives).
